class Event {
  String eventTitle;
  String eventSubTitle;
  String eventDate;
  String eventLocation;
  int eventPrice;

  Event(this.eventTitle, this.eventSubTitle, this.eventDate, this.eventLocation, this.eventPrice);
}